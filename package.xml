<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>bagwis_interfaces</name>
  <version>0.1.0</version>
  <description>
    Message, service and topic-name definitions crossing a BAGWIS node boundary.
    Contains no logic. Consumed as a pinned-tag git submodule by bagwis-airborne,
    bagwis-core and bagwis-web. See SRS-00.
  </description>

  <maintainer email="admin@prodigitality.net">Joshua</maintainer>
  <author>Joshua</author>
  <author>Earl Clyde</author>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>
  <buildtool_depend>ament_cmake_python</buildtool_depend>

  <!--
    Exactly the packages whose types appear in a msg/ or srv/ field:
      std_msgs           Header, Float64
      geographic_msgs    GeoPoint
      builtin_interfaces Time

    sensor_msgs, vision_msgs, geometry_msgs and mavros_msgs are deliberately NOT
    declared. They appear in the SRS-00 SS3 topic table, but no field here
    references them, so depending on them would force ros-humble-mavros-msgs and
    ros-humble-vision-msgs onto every machine that builds this package, the Pi
    Zero 2 W included, to satisfy a dependency nothing uses. Their types are
    recorded in topics/registry.yaml, so the contract is still written down once,
    and each consumer declares what it actually subscribes to.
  -->
  <depend>std_msgs</depend>
  <depend>geographic_msgs</depend>
  <depend>builtin_interfaces</depend>

  <exec_depend>rosidl_default_runtime</exec_depend>
  <member_of_group>rosidl_interface_packages</member_of_group>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
